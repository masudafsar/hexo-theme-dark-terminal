<% var cmd = site.pages.findOne({source: 'whoami/index.md'})
if (cmd){ %>
    <%- partial('components/command', {
        cmdData: {
            title: cmd.title,
            class: cmd.class,
            content: cmd.content,
        }
    }) %>
<% } %>
